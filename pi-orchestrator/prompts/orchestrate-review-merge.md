Use the orchestrator to implement, review, and integrate:

$ARGUMENTS

After worker completion, summarize results, run `orchestrator_verify`, ask me whether to integrate, then call `orchestrator_merge`. If conflicts or validation failures occur, stop and report the integration worktree path.
